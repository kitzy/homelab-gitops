apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backup-storage
  namespace: fleet-sandbox
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  volumeName: backup-storage-fleet-sandbox
  resources:
    requests:
      storage: 1Ti

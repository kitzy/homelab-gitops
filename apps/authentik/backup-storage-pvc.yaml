apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backup-storage
  namespace: authentik
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  volumeName: backup-storage
  resources:
    requests:
      storage: 1Ti

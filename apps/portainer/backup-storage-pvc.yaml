apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backup-storage
  namespace: portainer
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  volumeName: backup-storage-portainer
  resources:
    requests:
      storage: 1Ti

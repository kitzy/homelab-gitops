apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello
  namespace: hello-world
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - hello.tail26a1d8.ts.net
  defaultBackend:
    service:
      name: hello-world
      port:
        number: 443

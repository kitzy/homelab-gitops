apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backup-storage
  namespace: scanopy
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  volumeName: backup-storage
  resources:
    requests:
      storage: 1Ti
